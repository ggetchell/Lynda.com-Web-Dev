// Comments are used for expressing or telling info about either what the code does or what each line of code does, also gives others info about what it does.
// Comments are ignored by the program.
var a = 5;
var b = 10;
var c = 20; 
var x = true; // variable x is evaluated to TRUE
var z = false; // variable z is evaluated to FALSE
var name = prompt("What is your name?"); // prompts you to enter you name
var message = "Hello, ";
alert(message + name); // outputs the results of message + name